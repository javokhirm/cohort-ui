/**
 * The student's Notifications screen — reached from the app bar's bell rather than a tab
 * (`GET /student/notifications`). Layout only: the shell owns the app bar and the scrolling
 * content column.
 */
export function InboxRoute() {
	return <div className="mx-auto w-full max-w-180 pb-8" />;
}
