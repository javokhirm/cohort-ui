/**
 * The student's Billing screen — the balance summary (`GET /student/billing/summary`) over
 * the invoice list (`GET /student/invoices`). Layout only: the shell owns the app bar and
 * the scrolling content column.
 */
export function BillingRoute() {
	return <div className="mx-auto w-full max-w-205 pb-8" />;
}
