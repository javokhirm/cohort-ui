/**
 * The student's Progress screen — published results (`GET /student/results`) and the
 * attendance record (`GET /student/attendance`), split across two tabs. Layout only: the
 * shell owns the app bar and the scrolling content column.
 */
export function ProgressRoute() {
	return <div className="mx-auto w-full max-w-210 pb-8" />;
}
