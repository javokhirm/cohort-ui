/**
 * The student's Profile screen — reached from the app bar's avatar and the sidebar footer
 * rather than a tab (`GET /student/me`). Layout only: the shell owns the app bar and the
 * scrolling content column.
 */
export function ProfileRoute() {
	return <div className="mx-auto w-full max-w-160 pb-8" />;
}
