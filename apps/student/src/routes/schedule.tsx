/**
 * The student's Schedule screen — the week strip over the selected day's sessions
 * (`GET /student/sessions`). Layout only: the shell owns the app bar and the scrolling
 * content column; this is the column the screen's own content lands in.
 */
export function ScheduleRoute() {
	return <div className="mx-auto w-full max-w-210 pb-8" />;
}
